# Web3 Asset Dashboard

一个完整的 Web3 全栈示例项目，用于展示资产查询、合约交互以及链上数据展示功能（包括前端、后端、合约）💡

这个项目集成了：

- **智能合约**（Solidity + Hardhat）智能合约代码与部署脚本
- **后端服务**（Node.js + TypeScript + Express + ethers.js）API 服务，负责链上调用与数据聚合
- **前端界面**（Vue） 与用户交互的 Web3 UI

项目目标是帮助你快速搭建一个可运行的 Web3 dApp，并且覆盖典型的 Web3 项目功能，例如：  
钱包登录、资产查询、合约调用、事件监听等。

## 🚀 项目结构

```text
web3-asset-dashboard/
├─ contracts/        # Solidity 合约 + Hardhat 配置
├─ backend/          # Node.js TypeScript 后端服务
├─ frontend/         # 前端 UI Vue
└─ README.md         # 项目说明文档
```

## 🧪 本地开发流程

### 1️⃣ 启动本地区块链（Hardhat）

```bash
cd contracts
npx hardhat node
```

### 2️⃣ 部署合约

```bash
npx hardhat run scripts/deploy.ts --network localhost
```

### 3️⃣ 启动后端服务

```bash
cd backend
npm install
npm run dev
```

### 4️⃣ 启动前端界面

```bash
cd frontend
npm install
npm run dev
```

## 📌 贡献 & 交流

欢迎提出 issue 或 PR！
这个项目适合初学者巩固 Web3 栈开发能力 😉
