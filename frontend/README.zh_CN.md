# Frontend (React / Vue + Web3)

该目录为前端 Web3 UI，与智能合约交互。

## 功能

- 钱包连接（MetaMask）
- 调用合约（balance / mint / transfer）
- 显示链上资产

## 本地开发

### 安装依赖

```bash
cd frontend
npm install
```

### 环境变量

创建 .env：

```env
VITE_RPC_URL=http://127.0.0.1:8545
```

### 启动服务

```bash
npm run dev
```
